import styled from "styled-components";

export const AnimateContainerVolume = styled.div`
  width: 100%;
  height: 100%;
  background: rgb(204, 204, 204, 0.7);
  position: absolute;
  top: -2px;
  left: 0;
  pointer-events: none;
  z-index: 1;
  transform: translateY(-100%);
`;
